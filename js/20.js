// Operador ternario  -> condición ? si se cuple : si no se cumple

const disponible = 100
const total = 500
const tarjeta = true

disponible > total || tarjeta ? 
    console.log('Si puedes pagar') : 
    console.log('No puedes pagar')